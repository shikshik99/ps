#include <iostream>
#include <vector>
#include <algorithm>
#include <cstring>
using namespace std;

vector <int>v, ans;
bool chk[101];
int N;

bool foo(int idx){
    
}

int main(){
    cin >> N;
    v.resize(N+1);
    memset(chk, false, sizeof(chk));
    for(int i = 1; i < N+1; i++)cin >> v[i];
    foo(1);
}